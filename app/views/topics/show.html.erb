<div class="col-md-8 blog-main">
  <div class="pb-4 mb-4 font-italic border-bottom">
    <h1>
      <%= @topic.title %>
    </h1>
  </div>
  <nav class="blog-pagination">
    <%= paginate @blogs %>
  </nav>

  <%= render @blogs %>

  <nav class="blog-pagination">
    <%= paginate @blogs %>
  </nav>

</div>
